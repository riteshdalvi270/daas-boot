Create table ShipWreck(

    ID INT AUTO_INCREMENT,
    Name VARCHAR (255),
    Description VARCHAR(2000),
    Condition VARCHAR(255),
    Depth INT,
    Latitude DECIMAL,
    Longitude DECIMAL,
    Year_Discovered INT
);
